#!/bin/sh
# Run PHP with the JIT compiler enabled and training-sized memory (used for training runs).
exec php -d opcache.enable_cli=1 -d opcache.jit=tracing -d opcache.jit_buffer_size=128M -d memory_limit=1G "$@"
